from physics.particlesystem import ParticleSystem


# Collision Handler is a graphical concept to solve collision problems.
class CollisionHandler:
    def __init__(self):
        pass

    def __call__(self, pos, nextPos, **kwargs):
        return nextPos
